<?xml version="1.0" encoding="utf-8"?>
<!-- Splash-K brand mark as the launcher icon. Foreground IS the full kuira_logo
     (dark field + white K), so it fills the adaptive tile; background is a
     matching black for the mask's outer ring. -->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@drawable/ic_launcher_background" />
    <foreground android:drawable="@drawable/kuira_logo" />
</adaptive-icon>
