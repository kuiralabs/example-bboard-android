# Gradle
.gradle/
build/
out/
.kotlin/

# Android
*.apk
*.aab
*.dex
local.properties
.idea/
*.iml
captures/

# Signing material — never commit release keystores. Keep them outside the
# repo and reference via gradle.properties / env vars at build time.
*.keystore
*.jks
keystore.properties

# Compact / npm
node_modules/
npm-debug.log*
yarn-debug.log*

# OS
.DS_Store
Thumbs.db

# Contract assets are generated into app/src/main/assets/ from the source of
# truth (contract/src/managed/bboard) by the io.github.kuiralabs.contract plugin
# at build time — sourced, not stored.
app/src/main/assets/runtime/bboard-contract.js
app/src/main/assets/keys/
